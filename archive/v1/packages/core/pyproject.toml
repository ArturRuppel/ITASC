[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cellflow-core"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0",
    "tifffile>=2022.2.2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/cellflow"]
