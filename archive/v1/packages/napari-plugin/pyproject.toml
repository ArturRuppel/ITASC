[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cellflow-napari"
version = "0.1.0"
description = "CellFlow napari plugin"
requires-python = ">=3.9"
license = {text = "GPL-3.0"}
authors = [{name = "Artur Ruppel"}]
dependencies = [
    "napari[all]>=0.4.18",
    "qtpy>=2.3.0",
    "cellflow-analysis",
]

[project.entry-points."napari.manifest"]
cellflow = "cellflow:napari.yaml"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
correction = ["laptrack>=0.14.0"]
pipeline = ["cellflow-cellpose", "cellflow-ultrack", "cellpose>=3.1.0", "laptrack>=0.14.0"]
all = ["cellflow-napari[correction,pipeline]"]

[tool.hatch.build.targets.wheel]
packages = ["src/cellflow"]

[tool.hatch.build.targets.wheel.shared-data]
"src/cellflow/napari.yaml" = "cellflow/napari.yaml"
