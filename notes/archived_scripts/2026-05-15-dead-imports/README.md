# Archived scripts with stale imports

These scripts were removed from the active `scripts/` directory on 2026-05-15
because they referenced `cellflow` modules or symbols that no longer exist.
They are kept as text artifacts (`*.py.disabled`) so pytest and import checks do
not treat them as runnable project code.

