# Build on the Python API

```{figure} ../_static/diagrams/diagram-distro-core.svg
:alt: The ITASC ingredient board with only Core lit; every stage piece dashed.
:figclass: only-light
:width: 100%

**itasc-core** is the shared substrate every other distribution is built on: the file
conventions that make a project folder a source of truth, and the napari UI primitives the
stage widgets are assembled from. Solid chip is shipped; dashed are the rest of the family.
```
```{figure} ../_static/diagrams/diagram-distro-core-dark.svg
:alt: The ITASC ingredient board with only Core lit; every stage piece dashed.
:figclass: only-dark
:width: 100%

**itasc-core** is the shared substrate every other distribution is built on: the file
conventions that make a project folder a source of truth, and the napari UI primitives the
stage widgets are assembled from. Solid chip is shipped; dashed are the rest of the family.
```

```{include} ../../packages/itasc-core/README.md
:start-after: '# itasc-core'
```
