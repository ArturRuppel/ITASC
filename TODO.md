widget should open at the correct width right away

widgets should remind the text field from the relevant fields of the project widget instead of the long absolute paths. the relative paths of the required data, including file name, and wether or not it's available, and also the shape etc. just like in the project widget

in correction widget, when merging cells, it should be the selected cell that determines the cell id

in correction widget, load nuclear labels should also load cell zavg. layering should be cellzag at the bottom, then nucleus, then labels. nucleus with additive blending and bop_orang lut

Write a quick user guide to get the user oriented quickly. think about how and where to display it.

expose an overview of each widget somewhere. discrete but obvious. to be designed first

