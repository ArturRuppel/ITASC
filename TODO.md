implement accordion_layout for dataprep stage
import/export doesn't pull time and pixelsize information from the metadata
s01b and s01a in the tab names of cellpose can be removed, the other ones don't have the stage number either
cellpose cell segmentation should have a "run in terminal" button too
1_cellpose/nucleus in the data state widget shouldn't just show the number of files in the folder, but also validate that they have the right names and display the name of the first one with ... for the rest and display the shape of the first one too