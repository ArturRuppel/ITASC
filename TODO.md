loading tissue doesn't load metadata

activate in correction widget doesn't always find the active label layer. says it can't find it though it is clearly active. i need to press load on the segmentation loading button, just loading a tissue from h5 is not enough, even though the shape is being shown.
