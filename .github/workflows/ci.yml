name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  checks:
    name: ${{ matrix.os }} · Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Linux carries the full supported-Python sweep; Windows and macOS
        # each run one job on 3.12 (the version most scientific users are on,
        # and the least likely to be missing native wheels for torch/numba/
        # pymaxflow) so JOSS reviewers' `pip install` path is covered on every
        # OS without a 3x CI-minute bill.
        include:
          - os: ubuntu-latest
            python-version: "3.10"
          - os: ubuntu-latest
            python-version: "3.13"
          - os: windows-latest
            python-version: "3.12"
          - os: macos-latest
            python-version: "3.12"

    env:
      QT_QPA_PLATFORM: offscreen

    # Git Bash ships on the Windows runner, so a single shell keeps the
    # heredocs and line-continuations below identical across all three OSes.
    defaults:
      run:
        shell: bash

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install Qt/OpenGL system libraries
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libegl1 libopengl0 libgl1 libglib2.0-0 libdbus-1-3 \
            libxkbcommon0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-xinerama0 \
            libxcb-cursor0

      - name: Install package and tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[dev]
          python -m pip install build

      - name: Lint
        run: python -m ruff check .

      - name: Test
        run: python -m pytest

      - name: Build package
        run: python -m build --sdist --wheel --outdir dist

      - name: Build extracted distributions
        run: |
          python -m build --wheel packages/cellflow-core --outdir dist
          python -m build --wheel packages/cellflow-aggregate --outdir dist
          python -m build --wheel packages/cellflow-tracking --outdir dist
          python -m build --wheel packages/cellflow-cellpose --outdir dist

      - name: Verify isolated install (PEP 420 namespace + headless core)
        run: |
          python -m venv cf-verify
          # venv puts its scripts in bin/ on Linux+macOS, Scripts/ on Windows.
          VENV_BIN=cf-verify/bin
          [ -d "$VENV_BIN" ] || VENV_BIN=cf-verify/Scripts
          . "$VENV_BIN/activate"
          pip install --upgrade pip
          pip install h5py numpy scipy scikit-image tifffile sqlalchemy pydantic pandas "tomli>=2.0; python_version < '3.11'"
          pip install --no-deps dist/cellflow_core-*.whl dist/cellflow_aggregate-*.whl dist/cellflow_tracking-*.whl dist/cellflow_cellpose-*.whl
          python - <<'PY'
          import os, tempfile, numpy as np, tifffile, h5py
          import cellflow            # PEP 420 namespace, no orchestrator present
          import cellflow.core.tiff
          from cellflow.contact_analysis import build_contacts
          d = tempfile.mkdtemp()
          labels = np.zeros((2, 5, 6), dtype=np.uint16)
          labels[:, :, :3] = 1
          labels[:, :, 3:] = 2
          cp = os.path.join(d, "cells.tif")
          tifffile.imwrite(cp, labels)
          out = build_contacts(cell_labels_path=cp, output_path=os.path.join(d, "ca.h5"))
          with h5py.File(out) as h5:
              assert h5["cells/table/cell_id"][:].tolist() == [1, 2, 1, 2]
          print("isolated cellflow-aggregate install OK")

          # cellflow-tracking composes into the same namespace without the
          # orchestrator or the contact piece, and its headless backend imports.
          import cellflow.tracking_ultrack.db_build  # noqa: F401
          import cellflow.tracking_ultrack.validation_state as v
          import cellflow.correction.labels  # noqa: F401
          from cellflow.core.lineage import build_lineage
          from cellflow.core.cancellation import CancelledError  # noqa: F401
          from cellflow.tracking_ultrack.corrections import Correction
          nd = tempfile.mkdtemp()
          v.add_correction(nd, Correction(cell_id=7, t=0, kind="validated", y=1.0, x=2.0))
          assert v.read_validated_tracks(nd) == {7: {0}}
          assert os.path.exists(os.path.join(nd, "corrections.json"))
          lanes = build_lineage(labels)
          assert {lane.cell_id for lane in lanes.lanes} == {1, 2}
          print("isolated cellflow-tracking backend OK")

          # cellflow-cellpose composes into the same namespace on cellflow-core
          # alone. The Cellpose model lib is an optional extra (lazy import), so
          # the runner module imports without it, and the divergence-map builder
          # runs headlessly on synthetic prob/dp stacks.
          import cellflow.cellpose.cellpose_runner  # noqa: F401  (no cellpose lib needed to import)
          from cellflow.cellpose import build_divergence_maps
          cp_dir = tempfile.mkdtemp()
          prob = np.zeros((2, 2, 10, 10), dtype="float32")          # (T, Z, Y, X)
          dp = np.zeros((2, 2, 2, 10, 10), dtype="float32")         # (T, Z, 2, Y, X)
          prob_p = os.path.join(cp_dir, "nucleus_prob.tif")
          dp_p = os.path.join(cp_dir, "nucleus_dp.tif")
          tifffile.imwrite(prob_p, prob)
          tifffile.imwrite(dp_p, dp)
          report = build_divergence_maps(
              prob_p, dp_p,
              os.path.join(cp_dir, "nucleus_contours.tif"),
              os.path.join(cp_dir, "nucleus_foreground.tif"),
              foreground_z_reduction="mean",
              contour_z_reduction="mean",
              smoothing_sigma=0.0,
              median_radius=0,
          )
          assert report.frames == 2
          assert os.path.exists(os.path.join(cp_dir, "nucleus_foreground.tif"))
          print("isolated cellflow-cellpose backend OK")
          PY

      - name: Verify napari manifests validate
        run: |
          pip install npe2
          python - <<'PY'
          from npe2 import PluginManifest
          for path, name in [
              ("src/cellflow/napari.yaml", "cellflow"),
              ("src/cellflow/contact_analysis/napari.yaml", "cellflow-aggregate"),
              ("src/cellflow/tracking_ultrack/napari.yaml", "cellflow-tracking"),
              ("src/cellflow/cellpose/napari.yaml", "cellflow-cellpose"),
          ]:
              m = PluginManifest.from_file(path)
              assert m.name == name, (m.name, name)
              print("manifest OK:", m.name)

          # The full install folds each extracted piece's widget factory into the
          # single cellflow manifest; assert it mirrors the standalone factories.
          def factories(path):
              m = PluginManifest.from_file(path)
              return {c.python_name for c in m.contributions.commands}

          folded = factories("src/cellflow/napari.yaml")
          for path in (
              "src/cellflow/contact_analysis/napari.yaml",
              "src/cellflow/tracking_ultrack/napari.yaml",
              "src/cellflow/cellpose/napari.yaml",
          ):
              assert factories(path) <= folded, path
          print("folded manifest mirrors standalone factories OK")
          PY
